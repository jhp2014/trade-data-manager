# @trade-data-manager/db — 자급 .env. 레거시 public 과 같은 DB 인스턴스를 가리키되 스키마는 `market`.
# drizzle.config 이 이 파일을 로드하고 schemaFilter:["market"] 로 범위를 제한한다(public 안 건드림).
DATABASE_URL=postgresql://user:password@localhost:5432/trade-data-manager
