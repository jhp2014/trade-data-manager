{
  "name": "@trade-data-manager/market-data",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "db:generate": "echo 'Use feature-engine db:generate instead' && exit 1",
    "db:push": "echo 'Use feature-engine db:push instead' && exit 1",
    "db:studio": "echo 'Use feature-engine db:studio instead' && exit 1"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@trade-data-manager/tsconfig": "workspace:*",
    "@types/pg": "^8.20.0"
  }
}