# Dependency directories
node_modules/

# Build outputs
/dist/
/build/

# Environment variables
.env
.env.*
!.env.example

# Logs
logs/
*.log

# apps/live 알람 설정(watchlist+룰) — 사용자 로컬 상태
apps/live/data/

# Drizzle ORM — 마이그레이션은 git 으로 관리(ignore 안 함).

# OS generated files
.DS_Store
Thumbs.db

# Cache
.cache/

# csv
/csv

# Turborepo
.turbo

# Next.js
.next/
out/
build/

# TypeScript
*.tsbuildinfo

# ESLint
.eslintcache


# Utils for AI
/flattened-output

# API Key
chart-review-*.json

# 작업 중 만든 데이터 덤프 (레포에 둘 물건이 아니다 — 참조하는 코드가 없다)
tm.json
tm-codes.json

# Google OAuth / 서비스계정 자격증명 (절대 커밋 금지)
client_secret_*.json
*.apps.googleusercontent.com.json